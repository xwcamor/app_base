// Actualiza el contenido del modal
$('#modal').html("<%= j render :partial => @main_url + '/partials/modal_div', locals: { show_save_button: false, show_delete_button: false, show_cancel_button: false } %>");

// Muestra el modal
$('#main_modal').modal('show');