<tr>

  <th><%= sort_link(@query, :created_at, dynamic_translation('id'), { class: "text-dark" }) %></th>
 
  <th><%= sort_link(@query, :created_at, dynamic_translation('created_at'), { class: "text-dark" }) %></th>

  <th><%= sort_link(@query, :customer_id, dynamic_translation('customer_id'), { class: "text-dark" }) %></th>

  <th><%= sort_link(@query, :employee_id, dynamic_translation('employee_id'), { class: "text-dark" }) %></th>

  <th><%= sort_link(@query, :type_payment_id, dynamic_translation('type_payment_id'), { class: "text-dark" }) %></th>
  
  <th><%= sort_link(@query, :report_payment_id, dynamic_translation('report_payment_id'), { class: "text-dark" }) %></th>

  <th><%#= sort_link(@query, :user_name, dynamic_translation('name'), { class: "text-dark" }) %></th>

  <th><%#= sort_link(@query, :user_name, dynamic_translation('name'), { class: "text-dark" }) %></th>

  <th><%#= sort_link(@query, :user_name, dynamic_translation('name'), { class: "text-dark" }) %></th>

  <th><%= sort_link(@query, :user_lastname, dynamic_translation('lastname'), { class: "text-dark" }) %></th>
 
  <th><%= sort_link(@query, :active, dynamic_translation('active'), { class: "text-dark" }) %></th>
 
  <th><%= sort_link(@query, :active, t('standard.index_actions'), { class: "text-dark" }) %></th>

</tr>


      