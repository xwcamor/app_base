<tr>

  <th><%= sort_link(@query, :name, dynamic_translation('id'), { class: "text-dark" }) %></th>
 
  <th><%= sort_link(@query, :name, dynamic_translation('name'), { class: "text-dark" }) %></th>
 
  <th><%= sort_link(@query, :description, dynamic_translation('description'), { class: "text-dark" }) %></th>
 
  <th><%= sort_link(@query, :active, dynamic_translation('active'), { class: "text-dark" }) %></th>
 
  <th><%= sort_link(@query, :active, t('standard.index_actions'), { class: "text-dark" }) %></th>

</tr>